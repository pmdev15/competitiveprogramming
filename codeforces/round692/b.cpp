#include <bits/stdc++.h>
using namespace std;

void solve()
{
    long long int n;
    cin >> n;
    bool flag = false;
    int len = to_string(n).length();
    for (int i = 0; i < len; i++)
    {
        
    }
}
int main()
{
    int t = 1;
    cin >> t;
    while (t--)
    {
        solve();
    }
    return 0;
}